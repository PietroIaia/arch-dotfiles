(include "./bar/components/workspaces.yuck")
(include "./bar/components/settings.yuck")

;; Main bar widget
(defwidget bar [ws_start monitor]
  (centerbox
    :orientation "h"
    :class "taskbar"
    (workspaces :start ws_start :monitor monitor)
    (box)
    (settings)
  )
)

