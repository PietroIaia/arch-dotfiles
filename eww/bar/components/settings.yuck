(include "./bar/components/network.yuck")
(include "./bar/components/volume.yuck")
(include "./bar/components/battery.yuck")
(include "./bar/components/clock.yuck")
(include "./bar/components/power.yuck")

;; Settings widget
(defwidget settings []
  (box
    :class "widget top-right"
    :orientation "h"
    :space-evenly false
    :halign "end"
    (volume_button)
    (battery)
    (wifi_button)
    (clock)
    (power_button)
  )
)

