;; Clock widget
(defwidget clock []
  (box 
    :class "widget clock"
    :orientation "h"
    :space-evenly false
    :halign "end"
    (label
      :text "󰥔"
    )
    (label 
      :class "clock-label"
      :text "${formattime(EWW_TIME, "%I:%M%p")}"
    )
  )
)

